{
  "name": "fb-auth",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.114.0",
    "prettier": "2.4.1"
  },
  "dependencies": {
    "@aws-cdk/core": "1.114.0",
    "@serverless-stack/cli": "0.43.7",
    "@serverless-stack/resources": "0.43.7"
  }
}
